import { React } from "react";
import { CurrentPlatform } from "../components";

const Profile = () => {
  return (
    <div>
      <h1>Profile</h1>
      <CurrentPlatform />
    </div>
  );
};

export default Profile;
